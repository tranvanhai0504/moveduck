/** @type {import('next').NextConfig} */
const nextConfig = {
  // ... existing config ...
  images: {
    domains: ['plum-active-landfowl-217.mypinata.cloud'],
  },
  // ... existing config ...
}

module.exports = nextConfig